---
kind: Service
apiVersion: v1
metadata:
  name: post-service
  labels:
    app: post
spec:
  selector:
    app: post
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
...
---
apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: post-deployment
spec:
  selector:
    matchLabels:
      app: post
  replicas: 1
  template:
    metadata:
      labels:
        app: post
    spec:
      containers:
      - name: post
        image: viacheslavkr/post-service:2.0
        ports:
        - containerPort: 5000
        env:
        - name: DB_URL
          valueFrom:
            configMapKeyRef:
              name: services-address
              key: DB_URL
...